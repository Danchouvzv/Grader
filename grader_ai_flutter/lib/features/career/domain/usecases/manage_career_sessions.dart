class SaveCareerSession {
  const SaveCareerSession(this._repository);
  final dynamic _repository;
}

class LoadCareerSession {
  const LoadCareerSession(this._repository);
  final dynamic _repository;
}

class GetAvailableSessions {
  const GetAvailableSessions(this._repository);
  final dynamic _repository;
}

class DeleteCareerSession {
  const DeleteCareerSession(this._repository);
  final dynamic _repository;
}
